% Performes a paired ttest between the results for each fold
% of two different datasets. Accuracy, Final data size and accuracy are
% tested
function [] = ttestDatasets(totalStats1, totalStats2)
    
end